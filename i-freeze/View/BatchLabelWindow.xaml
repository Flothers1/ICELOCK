<Window x:Class="i_freeze.View.BatchLabelWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:utils="clr-namespace:i_freeze.Utilities"
        xmlns:viewmodel="clr-namespace:i_freeze.ViewModel"
        Title="BatchLabelWindow"
        Height="450"
        Width="800"
        utils:WindowCloser.DialogResult="{Binding DialogResult}"
        d:DataContext="{d:DesignInstance Type=viewmodel:BatchLabelWindowViewModel}"
        mc:Ignorable="d">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ItemsControl ItemsSource="{Binding Items}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="0,5">
                        <TextBlock Text="{Binding FileName}" Width="350"/>
                        <TextBox Text="{Binding Label}" Width="250" Margin="10,0,0,0"/>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="80" Margin="0,0,10,0"
                    Command="{Binding CancelCommand}"/>
            <Button Content="OK" Width="80"
                    Command="{Binding OkCommand}"/>
        </StackPanel>
    </Grid>
</Window>
